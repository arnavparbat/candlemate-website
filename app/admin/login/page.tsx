"use client";
import {FormEvent,useState} from "react";import {useRouter} from "next/navigation";import Link from "next/link";
export default function Login(){const [password,setPassword]=useState(""),[error,setError]=useState("");const router=useRouter();async function submit(e:FormEvent){e.preventDefault();const r=await fetch("/api/admin/login",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({password})});if(!r.ok)return setError("That password doesn’t match.");router.push("/admin")}return <main className="grain grid min-h-screen place-items-center p-5"><form onSubmit={submit} className="paper w-full max-w-sm rounded-3xl p-8"><Link href="/" className="inline-flex items-center gap-2.5 display text-2xl group"><img src="/logo.png" alt="Candlemate Logo" className="h-8 w-8 object-contain transition-transform group-hover:scale-105" /><span>candlemate<span className="text-gold">.</span></span></Link><h1 className="display mt-10 text-4xl">Studio login</h1><p className="mt-2 text-sm text-[#765442]">For the hands behind the glow.</p><input autoFocus type="password" value={password} onChange={e=>setPassword(e.target.value)} className="mt-7 w-full rounded-xl border border-[#8a61483a] bg-white px-3 py-3" placeholder="Admin password"/><p className="mt-2 min-h-5 text-sm text-red-700">{error}</p><button className="mt-3 w-full rounded-full bg-ink py-3 text-sm text-white">Enter studio</button></form></main>}
