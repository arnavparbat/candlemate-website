"use client";
import { Header } from "@/components/header";import { useCart } from "@/components/cart-context";import { Product } from "@/lib/types";import {useEffect,useState} from "react";import Link from "next/link";import {useParams} from "next/navigation";
export default function ProductPage(){const {id}=useParams<{id:string}>();const [product,setProduct]=useState<Product|null>(null);const [photo,setPhoto]=useState(0);const {add}=useCart();useEffect(()=>{fetch(`/api/products/${id}`).then(r=>r.json()).then(setProduct)},[id]);useEffect(()=>{if(!product||product.images.length<2)return;const t=setInterval(()=>setPhoto(p=>(p+1)%product.images.length),3000);return()=>clearInterval(t)},[product]);if(!product)return <><Header/><div className="p-20 text-center">Finding your candle…</div></>;return <><Header/><main className="mx-auto max-w-6xl px-5 py-10"><Link href="/" className="text-sm text-clay">← Back to collection</Link><div className="mt-6 grid gap-10 md:grid-cols-2"><div className="relative aspect-square overflow-hidden rounded-[32px] bg-[#ead1ad]"><img src={product.images[photo]} alt={product.name} className="h-full w-full object-cover"/><div className="absolute bottom-5 left-0 right-0 flex justify-center gap-2">{product.images.map((_,i)=><button key={i} onClick={()=>setPhoto(i)} className={`h-2 rounded-full ${i===photo?"w-6 bg-white":"w-2 bg-white/60"}`}/>)}</div></div><div className="py-4"><p className="text-xs font-bold uppercase tracking-[.2em] text-clay">{product.category}</p><h1 className="display mt-3 text-5xl">{product.name}</h1><p className="mt-5 text-2xl text-clay">₹{product.price}</p><p className="mt-7 max-w-lg leading-7 text-[#765442]">{product.description}</p><dl className="my-8 grid grid-cols-2 gap-5 border-y border-[#5c39271a] py-6 text-sm"><div><dt className="text-[#765442]">Burn time</dt><dd className="mt-1 font-medium">{product.burnTime}</dd></div><div><dt className="text-[#765442]">Made with</dt><dd className="mt-1 font-medium">{product.ingredients}</dd></div></dl><button disabled={!product.available} onClick={()=>add(product)} className="w-full rounded-full bg-ink px-6 py-4 text-sm text-cream disabled:cursor-not-allowed disabled:bg-stone-400">{product.available?"Add to bag":"Currently sold out"}</button></div></div></main></>}
